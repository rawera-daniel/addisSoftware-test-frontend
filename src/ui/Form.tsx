import styled from '@emotion/styled'

const Form = styled.form`
  padding: 2.4rem 4rem;
  border: 1px solid #f3f4f6;
  border-radius: 7px;

  overflow: hidden;
  font-size: 1.4rem;
`

export default Form
